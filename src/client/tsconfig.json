{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "rootDir": "./src",
        "outDir": "../www",
        "baseUrl": "./src",
        "paths": {
            "@root/*":["ts/*"],
            "@settings":["ts/settings"]
        }
    }
}